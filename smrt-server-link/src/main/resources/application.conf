# All the SMRT FLOW parameters are defined in the reference.conf
# This is just to override third-party level parameters

akka {
  loglevel = INFO
  log-dead-letters = off
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  jvm-exit-on-fatal-error = off
  #log-config-on-start = on
  actor {
    debug {
      # enable function of LoggingReceive, which is to log any received message at
      # DEBUG level
      #receive = on
      # enable DEBUG logging of all AutoReceiveMessages (Kill, PoisonPill et.c.)
      #autoreceive = on
    }
  }
  quartz {
    # Custom Schedules
    schedules {
      # This is really only for testing purposes
      Every30Seconds {
        description = "A cron job that fires off every 30 seconds"
        expression = "*/30 * * ? * *"
      }
      EverySaturdayMorning {
        description = "Run a backup every Saturday Morning between 3 and 5 AM"
        expression = "0 3-5 * ? 6 *"
        excludeWeekends = false
      }
    }
  }
}


spray.can {
  client {
    parsing {
      max-content-length: 64M
    }
  }
  server {
    registration-timeout: 10 s
  }
}
